# 로컬 추론 모델 경로는 각 실행 환경에 맞게 지정합니다.
ORDER_MODEL_BACKEND=kanana
KANANA_MODEL_ID=kakaocorp/kanana-2-3b-instruct
KANANA_MODEL_PATH=
KANANA_ADAPTER_PATH=

# 음성 인식을 쓰지 않을 때는 0으로 둡니다.
ASR_BACKEND=none
ASR_DEVICE=cuda
PROSODY_ENABLED=0

# 외부 연결 시에는 충분히 긴 임의의 값을 별도로 설정합니다.
LOCAL_INFERENCE_TOKEN=
